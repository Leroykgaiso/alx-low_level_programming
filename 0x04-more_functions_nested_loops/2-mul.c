#include "main.h"
#include <stdio.h>
/**
* mul - multiplies two integers
* @a: int
* @b: int
*
* Return: value of a * b
*/
int mul(int a, int b)
{
	int multiple;

	multiple = (a * b);
	return (multiple);
}
